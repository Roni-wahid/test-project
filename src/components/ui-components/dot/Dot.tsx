import {View, Text} from 'react-native';
import React from 'react';
import t from '../../../styles/theme';

const Dot = () => {
  return <View style={[t.w1, t.h1, t.roundedFull, t.bgGray600]}></View>;
};

export default Dot;
