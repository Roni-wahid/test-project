import {View, Text} from 'react-native';
import React from 'react';
import t from '../../../styles/theme';

const Divider = () => {
  return <View style={[t.borderB, t.borderGray500]}></View>;
};

export default Divider;
